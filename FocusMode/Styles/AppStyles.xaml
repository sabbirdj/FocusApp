<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Color Palette (Shadcn UI Dark Theme / Zinc) -->
    <!-- Background: #09090b -->
    <!-- Surface (Card): #18181b -->
    <!-- Border: #27272a -->
    <!-- Primary (Accent): #fafafa -->
    <!-- Primary Text: #fafafa -->
    <!-- Secondary Text: #a1a1aa -->

    <!-- SolidColorBrush resources -->
    <!-- Using fully opaque backgrounds to match shadcn flat look, abandoning Mica transparency for true Shadcn aesthetic -->
    <SolidColorBrush x:Key="AppBackgroundBrush" Color="#09090b" />
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#18181b" />
    <SolidColorBrush x:Key="CardBorderBrush" Color="#27272a" />
    
    <SolidColorBrush x:Key="AccentBrush" Color="#fafafa" />
    <SolidColorBrush x:Key="AccentSecondaryBrush" Color="#e4e4e7" />
    <SolidColorBrush x:Key="DangerBrush" Color="#ef4444" />
    <SolidColorBrush x:Key="SuccessBrush" Color="#22c55e" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#fafafa" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#a1a1aa" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="#27272a" />
    <SolidColorBrush x:Key="HoverBrush" Color="#27272a" />

    <!-- Card Style -->
    <Style x:Key="GlassCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="20" />
    </Style>

    <!-- Primary Button Style -->
    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="Foreground" Value="#18181b" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="Height" Value="40" />
        <Setter Property="Padding" Value="16,0" />
    </Style>

    <!-- Danger Button Style -->
    <Style x:Key="DangerButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource DangerBrush}" />
        <Setter Property="Foreground" Value="#fafafa" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="Height" Value="40" />
        <Setter Property="Padding" Value="16,0" />
    </Style>

    <!-- Ghost/Outline Button -->
    <Style x:Key="GhostButton" TargetType="Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="16,0" />
    </Style>

    <!-- Scan Button Style -->
    <Style x:Key="ScanButton" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="16,0" />
    </Style>

    <!-- Section Header Text -->
    <Style x:Key="SectionHeader" TargetType="TextBlock">
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
    </Style>

    <!-- Stat Value Text -->
    <Style x:Key="StatValue" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
    </Style>

    <!-- Stat Label Text -->
    <Style x:Key="StatLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}" />
        <Setter Property="CharacterSpacing" Value="60" />
    </Style>

</ResourceDictionary>
